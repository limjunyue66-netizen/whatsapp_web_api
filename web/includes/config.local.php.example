<?php
/**
 * Optional local overrides (base_url, app_env, session).
 * Database credentials: edit web/includes/database.php instead.
 *
 * Copy to config.local.php if needed. config.local.php is gitignored.
 */
return [
    'app_env' => 'production',
    'base_url' => 'https://YOUR-DOMAIN.com/whatsapp_web_api/web',
    'session' => [
        'secure' => true, // true when serving over HTTPS
    ],
];
