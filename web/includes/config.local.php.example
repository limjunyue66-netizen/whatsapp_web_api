<?php
/**
 * Optional local overrides (base_url, app_env, session).
 * Do NOT put database credentials here — use database.php.
 * On cPanel you can omit this file entirely.
 */
return [
    'app_env' => 'production',
    'base_url' => 'https://junyuelim.kolejsynergy.com/whatsapp_web_api/web',
    'session' => [
        'secure' => true,
    ],
];
